import Style from './Board.module.scss';

const Board = () => {
    return (
        <div id={Style.wrapper}>
            {/* <span>{user.name}</span> */}
            <br />
            {/* <span>{user.token}</span> */}
        </div>
    );
};

export { Board };
