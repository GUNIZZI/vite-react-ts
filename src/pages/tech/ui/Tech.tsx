import Style from './Tech.module.scss';

const Tech = () => {
    return (
        <div id={Style.wrapper}>
            {/* <span>{user.name}</span> */}
            <br />
            {/* <span>{user.token}</span> */}
        </div>
    );
};

export { Tech };
